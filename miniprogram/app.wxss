/**app.wxss**/
@tailwind base;
@tailwind components;
@tailwind utilities;

page {
  height: 100vh;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
}

@media (prefers-color-scheme: dark) {
  .invert-icon {
    filter: invert(1);
  }
}
